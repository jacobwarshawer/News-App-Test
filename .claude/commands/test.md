Run the server test suite and report the results.

Steps:
1. Run `cd server && npm test`
2. Report which test suites passed and which failed
3. For any failures, show the test name, the expected value, and the received value
4. End with a one-line summary: total suites, total tests, pass count, fail count
